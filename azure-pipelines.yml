trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
  displayName: Compile
  
- pwsh: |
    docker compose up -d
  workingDirectory: test/hello-sqlserver

- task: DotNetCoreCLI@2
  inputs:
    command: 'test'
